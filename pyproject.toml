[tool.poetry]
name = "tree-menu"
version = "0.1.0"
description = ""
authors = ["gabitovichb <gabitovichb@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
